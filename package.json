{
  "name": "clinicaltrial-vghtpe-refactor",
  "version": "0.2.0",
  "private": true,
  "description": "Public-safe refactor workspace for clinical-trial parsing, classification, search, import review, and persistence.",
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "engines": {
    "node": ">=18"
  }
}
